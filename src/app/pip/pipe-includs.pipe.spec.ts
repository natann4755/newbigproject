import { PipeIncludsPipe } from './pipe-includs.pipe';

describe('PipeIncludsPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeIncludsPipe();
    expect(pipe).toBeTruthy();
  });
});
